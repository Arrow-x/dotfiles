#!/bin/sh
dir="$1"
tags="$(cat "$(find "$dir" -maxdepth 1 -type f -name '*.txt' -print -quit)")"
find "$dir" -type f -name '*.txt' -delete
printf "%s" "$tags" | while read -r line; do
	tmsu tag "$dir" "$line"
done
