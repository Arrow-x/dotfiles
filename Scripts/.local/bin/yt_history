#!/bin/sh

choice="$(tac $HOME/.cache/yttitle | cut -d '|' -f2 | fzf)"

if [ -z "$choice" ]; then
	exit
fi

grep --fixed-strings "$choice" $HOME/.cache/yttitle | cut -d '|' -f1 | xargs link_handler &
